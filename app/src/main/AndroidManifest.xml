<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omega.mouthpiece">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mouth_Display"></activity>
        <activity
            android:name=".openWebsiteActivity"
            android:label="@string/title_activity_open_website"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
        <activity
            android:name=".mouthSelection"
            android:label="@string/title_activity_mouth_selection"
            android:theme="@style/AppTheme.NoActionBar"></activity>-
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>